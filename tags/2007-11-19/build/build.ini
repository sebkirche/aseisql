;this file will be used on exe compile

[version]

;Highest committed revision number
revisionNumber=$WCREV$

;Date of highest committed revision
revisionDate=$WCDATE$

;Repository URL of the working copy
;version will be taken from URL:
;trunk          = trunk       (latest version; nightly build?)
;branches/X.X   = X.Xb        (beta version)
;tags/X.X       = X.X         (pure version; branch must be merged and deleted)
revisionURL=$WCURL$
